import torch
import torch.utils.data
from torchvision import datasets, transforms
from torchvision.utils import save_image
import numpy as np
import matplotlib.pyplot as plt
import os
import time

from NP import ContextEncoder, ContextToDistrib, Decoder

def complete_full_image(decoder, context):
    return None

def complete_pixel(decoder, context, pixel):
    return None

def main():
    c
    return

if __name__ == "main":

    main()
